#' @param quiet a logical value, passed to [curl::curl_download()], which does
#' the downloading.  If this is TRUE, then the work is done quietly. Until
#' version version 0.1.12, this was called `silent`, but it was renamed to
#' match the corresponding argument in [curl::curl_download()].
